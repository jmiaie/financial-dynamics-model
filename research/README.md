# Research provenance (Directive #9)

| Artifact | Purpose |
|---|---|
| `holdout-audit.md` | CLEAR vs PREVIOUSLY INSPECTED for the 2025 holdout |
| `experiment-ledger.csv` | Append-only experiment register (tracked; exception to root `*.csv` gitignore) |

Raw vendor data lives in `data/raw/` (gitignored). Manifests/checksums live in `data/manifests/`.
